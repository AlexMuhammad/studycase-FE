{
  "dependencies": {
    "husky": "^8.0.3",
    "prettier": "^2.8.3"
  },
  "scripts": {
    "prepare": "husky install",
    "lint": "prettier --check .",
    "format": "prettier --write"
  },
  "devDependencies": {
    "lint-staged": "^13.1.0"
  }
}
