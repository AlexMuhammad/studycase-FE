{
  "devDependencies": {
    "husky": "^8.0.0"
  },
  "scripts": {
    "prepare": "husky install",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "prettier": "^2.8.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "prettier --write"
    }
  },
  "lint-staged": {
    "*": "prettier --write"
  }
}
